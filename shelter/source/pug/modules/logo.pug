a.header__logo.logo(href=pageClass === 'index-page' ? "#" : 'index.html')(class=pageClass === 'index-page' ? "logo--index" : '')
  p.logo__title Cozy House
  p.logo__subtitle Shelter for pets in Boston
